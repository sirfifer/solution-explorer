# Waived surfaces: enumerated obligations no Phase 1 dataset or tooling can
# exercise. A waiver is a visible decision, printed on every check run,
# never a silent gap. Each waiver names why, and what would retire it.

- token: edge:action
  reason: >
    No UIAction with a target_view exists in any Phase 1 dataset, so the Flow
    lens synthesizes no action edges. Retire with a fixture carrying a
    tap-to-navigate action.
- token: edge:cache
  reason: "No cache relationship in any Phase 1 dataset. Retire with a fixture or the Phase 4 demo dataset."
- token: edge:database
  reason: "No database relationship in any Phase 1 dataset. Retire with a fixture or the Phase 4 demo dataset."
- token: edge:event_bus
  reason: "No event_bus relationship in any Phase 1 dataset. Retire with a fixture or the Phase 4 demo dataset."
- token: edge:embed
  reason: >
    No embed (view composition) relationship in any Phase 1 dataset; the
    SwiftUI embed detector has no SwiftUI fixture with embedded views here.
    Retire with a SwiftUI fixture.
- token: edge:ffi
  reason: "No ffi relationship in any Phase 1 dataset. Retire with a fixture or the Phase 4 demo dataset."
- token: edge:file
  reason: "No file relationship in any Phase 1 dataset. Retire with a fixture or the Phase 4 demo dataset."
- token: edge:grpc
  reason: "No grpc relationship in any Phase 1 dataset. Retire with a fixture or the Phase 4 demo dataset."
- token: edge:message_queue
  reason: >
    The dogfood carries a message_queue CAPABILITY but no message_queue
    relationship edge. Retire with a fixture or the Phase 4 demo dataset.
- token: edge:pubsub
  reason: "No pubsub relationship in any Phase 1 dataset. Retire with a fixture or the Phase 4 demo dataset."
- token: edge:tab
  reason: "No tab-bar relationship in any Phase 1 dataset (the storyboard fixture has no TabView). Retire with a tabbed fixture."
- token: edge:websocket
  reason: "No websocket relationship in any Phase 1 dataset. Retire with a fixture or the Phase 4 demo dataset."
- token: subview:flow/which-tab
  reason: >
    The which-tab gesture reads tab edges in a flow; no Phase 1 dataset has a
    tab-bar flow. Retire together with edge:tab.
- token: subview:rules/governs-data
  reason: >
    The documented gesture needs a rule carrying an entity link ('view in
    Data lens'); zero of the dogfood's 204 rules carry entity_id. Data
    instance gap, recorded in the Phase 2 report; retire with a fixture whose
    io rules bind entities.
- token: subview:rules/guards-contract
  reason: >
    The documented gesture needs a rule carrying a capability link; zero of
    the dogfood's rules carry capability_id. Same disposition as
    governs-data.
- token: component:ToursEntry
  reason: >
    Tours live in ai_enhance.tours and no Phase 1 dataset carries any (the
    committed AI baseline has none), so the entry point never renders.
    Recorded discrepancy: the design's 'one full tour' sample has no data to
    run against. Retire by enriching the dogfood with a tour.
- token: component:TourPlayer
  reason: "Same as ToursEntry: no tours data anywhere in Phase 1."
- token: component:ErrorBoundary
  reason: >
    The design's 'induced render fault' has no user-reachable trigger in the
    production build, and the runner's closed action vocabulary is
    user-gestures only. Recorded discrepancy: exercising ErrorBoundary needs
    a fault-injection affordance (a dev-only crash trigger) or a scripted
    test harness. The malformed-dataset half of V12 is covered (V12.1).
- token: component:StatusDashboard
  reason: >
    Requires architecture.live_status, which only a live fixture provides;
    live surfaces are Phase 4 scope by design-doc decision.
- token: component:SolutionIndex
  reason: >
    Requires a composed multi-repo solution dataset, which is not in the
    Phase 1 dataset set. Recorded deferral to the multi-repo engagement.
- token: lens:design
  reason: >
    No committed GUI dataset is generated with --design-signals, so the
    Design lens never appears in the runner (the lens gates on the dataset
    carrying design_signals). Found by the 2026-08-21 verification round:
    the D-series build added the lens without plan coverage and never ran
    this check. Retire by regenerating the dogfood dataset with
    --design-signals and writing executed cases for the lens and each
    subview below.
- token: subview:design/where-is-it-weak
  reason: "Same as lens:design: no dataset carries design_signals. Retire with the design-signals dogfood regeneration."
- token: subview:design/locked-together
  reason: "Same as lens:design. The case needs a dataset with at least one dependency cycle; the dogfood has import cycles, so the regenerated dataset covers it."
- token: subview:design/load-bearing
  reason: >
    Same as lens:design, plus a data caveat: zone findings need abstractness,
    which is measured only in abstraction-capable languages, so the case must
    target the dogfood's TypeScript components.
- token: subview:design/how-do-we-know
  reason: "Same as lens:design: the method chips and the caveat footer render only when the lens does."
- token: subview:design/blast-radius
  reason: "Same as lens:design: the mode toggle lives in the Design panel, which mounts only under the lens."
- token: subview:design/seams
  reason: "Same as lens:design: the boundary-strength summary is a Design panel row."
- token: component:DesignPanel
  reason: "Same as lens:design: the panel mounts only under the Design lens. Retire together with the lens waiver."
- token: lens:support
  reason: >
    Exercised by the deterministic crawl (surfaces.spec.ts checks the lens
    switcher offers exactly the lenses the data warrants, and that every
    present entry opens a surface with something in it), but no AI-plan case
    is written yet: the plan has no Overview vector, and support is one of the
    two human-view lenses that shipped with it. Retire by authoring V5 rows
    for the lens and each subview below.
- token: subview:support/attention
  reason: "Same as lens:support: the ranked-attention list is a SupportPanel section, walked by the crawl's surface entry, not by a plan case."
- token: subview:support/configuration
  reason: "Same as lens:support: the configuration group renders only under the lens."
- token: subview:support/external
  reason: "Same as lens:support: the external-reliance group renders only under the lens."
- token: subview:support/entry
  reason: >
    Same as lens:support. The entry-point gesture is reached from Overview's
    question routes, which overview.spec.ts walks; the AI plan has no Overview
    vector to walk them from.
- token: lens:security
  reason: >
    Exercised by the deterministic crawl on the same two surfaces.spec.ts
    checks as lens:support (lens presence and surface entry), and no AI-plan
    case is written yet for the same reason: no Overview vector. Retire by
    authoring V5 rows for the lens and each subview below.
- token: subview:security/mechanisms
  reason: "Same as lens:security: the confirmed-mechanisms group is a SecurityPanel section, walked by the crawl's surface entry, not by a plan case."
- token: subview:security/boundaries
  reason: "Same as lens:security: the communication-boundaries group renders only under the lens."
- token: subview:security/credentials
  reason: "Same as lens:security: the credential-inputs group renders only under the lens; values are never displayed."
- token: subview:security/unknown
  reason: "Same as lens:security: the not-observable ledger is a SecurityPanel section under the lens."
- token: component:SystemOverview
  reason: >
    Exercised by the deterministic crawl (overview.spec.ts walks the three
    directions, the question routes and their continues, and the portrait
    cards). The AI plan has no Overview vector yet, so no case claims it.
    Retire by authoring one.
- token: component:ExperienceSwitcher
  reason: >
    Exercised by the deterministic crawl (overview.spec.ts moves between
    Overview and Workbench through open-workbench and
    open-overview). No AI-plan case yet; retire with the Overview vector.
- token: component:ViewerPreferences
  reason: >
    Exercised by the deterministic crawl (overview.spec.ts opens the
    preferences drawer and switches interface from it). No AI-plan case yet;
    retire with the Overview vector.
- token: component:ThemeSwitcher
  reason: >
    The dress popover in the Overview header. Its mount is walked by the
    deterministic crawl with the header; no AI-plan case exercises a theme
    change yet. Retire with the Overview vector, which is where the theme and
    variant controls live.
- token: component:TrustLedger
  reason: >
    Exercised by the deterministic crawl (overview.spec.ts opens the trust
    drawer from the ledger entry and closes it, asserting nothing changed).
    No AI-plan case yet; retire with the Overview vector.
- token: component:WorkbenchTrustStrip
  reason: >
    The compact chip row carrying the ledger, findings, supply-chain and tours
    entries in Workbench. Exercised by the deterministic crawl
    (surfaces.spec.ts opens every present entry). No AI-plan case claims the
    strip itself; retire with the Overview vector.
- token: component:SupportPanel
  reason: "Same as lens:support: the panel mounts only under the Support lens. Retire together with the lens waiver."
- token: component:SecurityPanel
  reason: "Same as lens:security: the panel mounts only under the Security lens. Retire together with the lens waiver."
