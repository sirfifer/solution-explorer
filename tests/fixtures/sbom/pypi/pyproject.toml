[project]
name = "pypi-fixture"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
  "requests>=2.28",
  "click==8.1.7",
  "PyYAML",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]
