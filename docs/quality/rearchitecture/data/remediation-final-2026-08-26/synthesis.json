{
  "tours": [
    {
      "id": "coverage-walk",
      "title": "Language coverage walk",
      "description": "Visits every planted language component in importance order to verify the analyzer's tree-sitter path fired for each one. A missing stop here is an analyzer bug, not fixture noise.",
      "steps": [
        {
          "target": "services/api",
          "title": "Python via FastAPI",
          "narration": "Start where the Python detection path is exercised: server.py exposes a GET /users/{user_id} handler that gives the analyzer a real FastAPI capability to detect. Every component on this walk exists to exercise a parser, not to do real work.",
          "evidence": {
            "file": "services/api/api/server.py",
            "line": 1
          }
        },
        {
          "target": "services/web/src",
          "title": "TypeScript and JavaScript",
          "narration": "The web client covers two languages at once: client.ts for TypeScript and format.js for JavaScript. It also carries the fixture's only outbound edge, which the next tour examines; here it just needs to appear with both languages attributed.",
          "evidence": {
            "file": "services/web/src/client.ts",
            "line": 1
          }
        },
        {
          "target": "services/worker",
          "title": "Go with a main entry point",
          "narration": "A single main.go provides valid Go syntax plus a main entry point for entry-point detection. Its zero edges are fixture design: it exists to be parsed, not called.",
          "evidence": {
            "file": "services/worker/main.go",
            "line": 1
          }
        },
        {
          "target": "apps/ios",
          "title": "Swift and SwiftUI",
          "narration": "The fixture's only Swift component. If it is missing from the map, the Swift/SwiftUI detection path went untested \u2014 which is why a band-3 component carries the same evidentiary weight as the band-1 api here.",
          "evidence": {
            "file": "apps/ios/Sources/App/ContentView.swift",
            "line": 1
          }
        },
        {
          "target": "libs/core",
          "title": "Rust and TOML",
          "narration": "Cargo.toml plus lib.rs form a minimal but valid crate \u2014 24 lines by design, per the README's 'keep it tiny' constraint. Its isolation exercises Rust parsing, nothing more.",
          "evidence": {
            "file": "libs/core/src/lib.rs",
            "line": 1
          }
        },
        {
          "target": "libs/rubylib",
          "title": "Ruby",
          "narration": "One 21-line parser.rb covers the Ruby path. Like the Rust crate before it, its under-implementation is intentional; do not read abandonment into a fixture built to parse fast in CI.",
          "evidence": {
            "file": "libs/rubylib/lib/parser.rb",
            "line": 1
          }
        },
        {
          "target": "root",
          "title": "Markdown and YAML at the root",
          "narration": "The walk ends where the config languages live: README.md and docker-compose.yml. This component also carries the fixture's only git activity and its most suspect mechanical signal, which the next tour takes up.",
          "evidence": {
            "file": "README.md",
            "line": 1
          }
        }
      ]
    },
    {
      "id": "one-edge",
      "title": "The fixture's one relationship",
      "description": "Follows the single declared cross-component edge \u2014 web client to api over HTTP \u2014 which is 100% of the relationship-detection evidence the fixture provides.",
      "steps": [
        {
          "target": "services/web",
          "title": "The declared package",
          "narration": "package.json establishes the web client's package identity. It is the declared half of the relationship; the actual call lives one level down.",
          "evidence": {
            "file": "services/web/package.json",
            "line": 1
          }
        },
        {
          "target": "services/web/src",
          "title": "The outbound call",
          "narration": "client.ts implements the request that targets services/api over HTTP. This is the only outbound edge in the entire fixture, so the mechanical 'zone of uselessness' finding here \u2014 nothing consumes the web client \u2014 is fixture design, not a defect.",
          "evidence": {
            "file": "services/web/src/client.ts",
            "line": 1
          }
        },
        {
          "target": "services/api",
          "title": "The receiving endpoint",
          "narration": "The edge terminates at the FastAPI handler in server.py, bound to port 8000. The boundary-strength signal says this one seam is separated by a real contract, which is exactly what the fixture was planted to make detectable.",
          "evidence": {
            "file": "services/api/api/server.py",
            "line": 1
          }
        }
      ]
    },
    {
      "id": "signals-to-adjudicate",
      "title": "Where mechanical signals need a second look",
      "description": "Two places where the analyzer's mechanical signals could mislead a parity diff: a port claim attributed to a markdown root, and edge counts that stop short of naming the caller.",
      "steps": [
        {
          "target": "root",
          "title": "The port that isn't here",
          "narration": "The analyzer records 'listens on a port' on a component whose only files are README.md and docker-compose.yml. The port is an attribution from the compose file's declared db/cache services, not a root-level bind \u2014 freeze it unexplained and the parity snapshot preserves an artifact.",
          "evidence": {
            "file": "docker-compose.yml",
            "line": 1
          }
        },
        {
          "target": "services/api",
          "title": "What the edge count actually proves",
          "narration": "Adjudication found the api's own evidence shows one inbound edge and one capability, but does not by itself name services/web/src as the caller or prove the api is unique in either respect. The relationship is real; the superlatives around it were not evidenced. This is what deterministic grounding looks like in practice.",
          "evidence": {
            "file": "services/api/api/server.py",
            "line": 1
          }
        }
      ]
    }
  ],
  "lenses": [
    {
      "name": "Component-local evidence cannot support fixture-global claims",
      "observation": "All four adjudication rejections on services/api share one shape: a claim scoped to the whole fixture ('only capability', 'highest inbound count', 'the caller is services/web/src') backed by evidence scoped to one component. The enrichment per-component context structurally cannot ground the cross-component claims the parity anchor most needs.",
      "why_it_matters": "An analyzer developer diffing P4-7 against this baseline needs the web\u2192api edge stated with its endpoints, but every attempt to state it at the component level was correctly struck down. The system-level relationship record, not component enrichment, is the only place that claim can be grounded \u2014 and the map should draw it from there.",
      "confidence": "high",
      "work_order": {
        "scope": [
          "services/api",
          "services/web/src"
        ],
        "lens": "Ground the web\u2192api edge from the system-level relationship record (source, target, protocol) rather than from either component's local facts, and rewrite both components' place/why_matters claims to cite it.",
        "criteria": "The relationship appears once, named with direction (services/web/src \u2192 services/api) and protocol (HTTP), cited to the relationship record; zero adjudication rejections remain on services/api.",
        "expected_effect": "truth: the adjudicator's unsupported-claim count on services/api drops from 4 to 0 and the grounded fraction of the census rises",
        "budget": {
          "max_cost_usd": 0.5,
          "max_targets": 2
        }
      }
    },
    {
      "name": "Importance banding inverts the fixture's own value ordering",
      "observation": "The mechanical bands rank services/api first by fan-in and push the Swift, Rust, and Ruby components to bands 3\u20135, but in a coverage fixture each language component is worth exactly one parser path \u2014 a dropped libs/rubylib is the same severity of analyzer bug as a dropped services/api. The census (3 grounded at opus, 6 at sonnet) suggests effort followed the bands rather than the coverage goal.",
      "why_it_matters": "A parity diff reader scans for missing languages, not for load-bearing components; a map that spends its confidence on the head and leaves tail components thinly grounded protects exactly the wrong thing.",
      "confidence": "medium",
      "work_order": {
        "scope": [
          "apps/ios",
          "libs/core",
          "libs/rubylib",
          "services/worker"
        ],
        "lens": "Verify each tail component's language attribution and file citations to the same grounding standard the band-1 components received \u2014 one checked file citation per language claimed.",
        "criteria": "Every code language in the tree maps to exactly the component the README assigns it, with a grounded file citation on each; the s2 check passes with no language attributed by inference alone.",
        "expected_effect": "truth: grounded fraction across band-3/4/5 components reaches parity with band-1",
        "budget": {
          "max_cost_usd": 0.6,
          "max_targets": 4
        }
      }
    }
  ],
  "work_orders": [
    {
      "scope": [
        "services/api",
        "services/web/src"
      ],
      "lens": "Ground the web\u2192api edge from the system-level relationship record (source, target, protocol) rather than from either component's local facts, and rewrite both components' place/why_matters claims to cite it.",
      "criteria": "The relationship appears once, named with direction (services/web/src \u2192 services/api) and protocol (HTTP), cited to the relationship record; zero adjudication rejections remain on services/api.",
      "expected_effect": "truth: the adjudicator's unsupported-claim count on services/api drops from 4 to 0 and the grounded fraction of the census rises",
      "budget": {
        "max_cost_usd": 0.5,
        "max_targets": 2
      },
      "issued_by": "P4",
      "outcome": null
    },
    {
      "scope": [
        "apps/ios",
        "libs/core",
        "libs/rubylib",
        "services/worker"
      ],
      "lens": "Verify each tail component's language attribution and file citations to the same grounding standard the band-1 components received \u2014 one checked file citation per language claimed.",
      "criteria": "Every code language in the tree maps to exactly the component the README assigns it, with a grounded file citation on each; the s2 check passes with no language attributed by inference alone.",
      "expected_effect": "truth: grounded fraction across band-3/4/5 components reaches parity with band-1",
      "budget": {
        "max_cost_usd": 0.6,
        "max_targets": 4
      },
      "issued_by": "P4",
      "outcome": null
    }
  ],
  "rejected_orders": [],
  "rejected_tours": [],
  "narrative_written": true,
  "notes": [
    "3 tour(s) written, 2 lens(es) discovered, 2 work order(s) issued"
  ]
}